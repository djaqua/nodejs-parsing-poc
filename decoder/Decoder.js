module.exports = function(options) {
      
    const get = (recordStr) => {
        return recordStr.substr();
    };


    const get = (recordStr) => {
        return recordStr.substr();
    };


    const get = (recordStr) => {
        return recordStr.substr();
    };


    return {
        decode: (recordStr, parser) => {
            parser.parse({
                : get(recordStr),        
                : get(recordStr),        
                : get(recordStr),        
            });
        },
    };
};

